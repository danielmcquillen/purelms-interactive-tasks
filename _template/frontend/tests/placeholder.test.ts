/**
 * TODO: rename to <your-slug>.test.ts and write tests for your
 * frontend bundle. The echo fixture (echo/frontend/tests/echo.test.ts)
 * is a worked example using vitest + happy-dom.
 */

import { describe, expect, it } from "vitest";

describe("placeholder", () => {
  it("is a placeholder", () => {
    expect(true).toBe(true);
  });
});
